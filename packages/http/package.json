{
  "name": "@ai-workbench/http",
  "version": "0.0.0",
  "private": true,
  "description": "Effect-native HttpClient boundary (@effect/platform FetchHttpClient) for one-attempt timeout, abort, status classification, and sanitized transport errors",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@ai-workbench/errors": "workspace:*",
    "@effect/platform": "0.96.2",
    "effect": "3.21.4"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json"
  }
}
